/** Minutes between consecutive public booking start options (fixed grid). */
export const BOOKING_SLOT_GRID_MINUTES = 15;

/** Minutes of buffer after a Google Calendar event when checking conflicts (widget only). */
export const BOOKING_SLOT_BUFFER_MINUTES = 15;
