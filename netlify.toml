# Allow the app to be embedded in iframes (for the Embed feature). Applied to all routes so it works with SPA routing.
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "frame-ancestors *"
